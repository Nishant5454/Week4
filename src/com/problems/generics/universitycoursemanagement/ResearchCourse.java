package com.generics.universitycoursemanagement;

public class ResearchCourse extends CourseType{
    // constructor to initialize values
    public ResearchCourse(String subject, String duration) {
        super(subject, duration);
    }
}
